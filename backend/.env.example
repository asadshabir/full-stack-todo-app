# Database Configuration
# IMPORTANT: Replace this with your actual Neon PostgreSQL connection string
# Get it from: https://console.neon.tech > Your Project > Connection Details
#
# Format for psycopg2: postgresql://USERNAME:PASSWORD@HOST/DATABASE?sslmode=require
#
# NOTE: If you see "password authentication failed" errors:
# 1. Go to https://console.neon.tech
# 2. Select your project
# 3. Go to "Connection Details" or "Dashboard"
# 4. Copy the connection string (use the one starting with postgresql://)
# 5. Update the DATABASE_URL below
#
DATABASE_URL=postgresql://your_user:your_password@your_host.neon.tech/your_database?sslmode=require

# JWT Configuration
# Generate a secure secret key using: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# CORS Configuration
# Comma-separated list of allowed origins
# Add your Vercel deployment URL here after deployment
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Server Configuration
PORT=8000
